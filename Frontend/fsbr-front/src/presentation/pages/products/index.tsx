

export default function ListProductsPage() {
    return (
        <div className="flex h-full justify-center items-center text-white text-xl">
            <p>Hi, this is the products list</p>
            {/* {error ? (
                <>Algo deu errado.</>
            ) : isLoading ? (
                <section>Carregando...</section>
            ) :
                (
                    <p>Ola, isso é um teste</p>
                )} */}
        </div>
    )
}